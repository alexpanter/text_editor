#include "context/applicationContext.hpp"

int main()
{
    ApplicationContext* application; // TODO: use a pointer instead?

    while(application->IsRunning()) {
        // TODO: do some things here!
    }

    return 0;
}
